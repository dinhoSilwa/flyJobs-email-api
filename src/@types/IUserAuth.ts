export interface IUserAuthSignup {
  email: string;
  password: string;
  security: boolean;
  // createdAt?: Date;
  // updatedAt?: Date;
}

export interface IUserAuthLogin {
  email: string;
  password: string;
  // createdAt?: Date;
  // updatedAt?: Date;
}
